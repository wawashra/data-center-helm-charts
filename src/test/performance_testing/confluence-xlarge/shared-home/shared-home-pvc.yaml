apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: confluence-xlarge-shared-home
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: confluence-xlarge-shared-home
  volumeMode: Filesystem
  volumeName: confluence-xlarge-shared-home
  resources:
    requests:
      storage: 1000Gi