apiVersion: v1
kind: PersistentVolume
metadata:
  namespace: bitbucket
  name: nfs
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 192.168.93.52 #IP of NFS Server POD
    path: "/srv/nfs"