apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: confluence-local-home-pvc-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  volumeMode: Filesystem
  volumeName: confluence-local-home-pv-0
  storageClassName: "local-home"
