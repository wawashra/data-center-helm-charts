apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jira-shared-home
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: jira-shared-home
  volumeMode: Filesystem
  volumeName: jira-shared-home
  resources:
    requests:
      storage: 1000Gi