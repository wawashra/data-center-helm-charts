apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-home-dylan
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: shared-home-dylan
  resources:
    requests:
      storage: 500Mi
