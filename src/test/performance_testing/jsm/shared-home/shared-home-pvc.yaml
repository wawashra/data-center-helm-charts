apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jsm-shared-home
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: jsm-shared-home
  volumeMode: Filesystem
  volumeName: jsm-shared-home
  resources:
    requests:
      storage: 1000Gi