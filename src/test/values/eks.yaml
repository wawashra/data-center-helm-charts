apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: ga-blitz-confluence
  region: ap-southeast-1

managedNodeGroups:
  - name: confluence
    instanceType: m5.2xlarge
    desiredCapacity: 2
    ssh:
      enableSsm: true
